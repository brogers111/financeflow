// Mock PDF.js worker for Next.js compatibility
// This prevents the worker loading error while running in main thread
if (typeof window !== "undefined") {
  console.log("PDF.js mock worker loaded");
}
